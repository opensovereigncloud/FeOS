[package]
name = "feos"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
enum_glob_use = "deny"

[dependencies]
futures = "0.3.29"
netlink-packet-route = "0.19.0"
rtnetlink = "0.14.0"

nix = { version = "0.28.0", features = ["mount", "user", "reboot", "feature", "net"] }
tokio = { version = "1.35.0", features = ["full", "macros", "rt-multi-thread"] }
log = "0.4.20"
simple_logger = "5.0.0"
tonic = "0.11"
prost = "0.12"
bitflags = "2.5.0"
uuid = { version = "1.8.0", features = ["v4"] }
oci-distribution = "0.11.0"
serde = "1.0.197"
serde_json = "1.0.115"
api_client = {branch = "main",  git = "https://github.com/cloud-hypervisor/cloud-hypervisor"  }
net_util = {branch = "main",  git = "https://github.com/cloud-hypervisor/cloud-hypervisor"  }
vmm = { branch = "main",  git = "https://github.com/cloud-hypervisor/cloud-hypervisor", features = ["kvm"] }
openssl = { version = "0.10", features = ["vendored"] }
dhcproto = "0.11"
rand = "0.8"
pnet = "0.34.0"

[build-dependencies]
tonic-build = "0.11.0"